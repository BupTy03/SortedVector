#pragma once
#ifndef TYPELIST_UTILS_HPP
#define TYPELIST_UTILS_HPP

/*

// count types metafunction
template<typename... Types>
struct count_types;

template<>
struct count_types<> { static constexpr std::size_t value = 0; };

template<typename T, typename... Types>
struct count_types<T, Types...> { static constexpr std::size_t value = 1 + count_types<Types...>::value; };

*/

#endif // !TYPELIST_UTILS_HPP
